<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Теплый Дом - Строительные материалы и утеплители{% endblock %}</title>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'background': '#ffffff',
                        'foreground': '#1a1a1a',
                        'card': '#ffffff',
                        'card-foreground': '#1a1a1a',
                        'primary': '#030213',
                        'primary-foreground': '#ffffff',
                        'secondary': '#f3f4f6',
                        'secondary-foreground': '#030213',
                        'muted': '#ececf0',
                        'muted-foreground': '#717182',
                        'accent': '#e9ebef',
                        'accent-foreground': '#030213',
                        'destructive': '#d4183d',
                        'destructive-foreground': '#ffffff',
                        'border': 'rgba(0, 0, 0, 0.1)',
                        'input': 'transparent',
                        'input-background': '#f3f3f5',
                        'ring': '#6b7280'
                    },
                    borderRadius: {
                        'lg': '0.625rem'
                    }
                }
            }
        }
    </script>

    {% block stylesheets %}{% endblock %}
</head>
<body class="min-h-screen bg-gray-50 flex flex-col">
<!-- Header -->
<header class="bg-white border-b border-gray-200">
    <!-- Top bar with contact info -->
    <div class="bg-red-600 text-white py-2">
        <div class="container mx-auto px-4 flex justify-between items-center">
            <div class="flex items-center gap-6">
                <div class="flex items-center gap-2">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                    </svg>
                    <span class="text-sm">Москва</span>
                </div>
            </div>
            <div class="flex items-center gap-6">
                <div class="flex items-center gap-2">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                    </svg>
                    <span class="text-sm">+7 (495) 545-39-00</span>
                </div>
                <span class="text-sm">Заказать звонок</span>
            </div>
        </div>
    </div>

    <!-- Main header -->
    <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
            <!-- Logo -->
            <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-red-600 rounded-lg flex items-center justify-center">
                    <div class="text-white text-xl">🏠</div>
                </div>
                <div>
                    <h1 class="text-xl font-bold text-gray-900">
                        <a href="{{ path('index') }}">Теплый Дом</a>
                    </h1>
                </div>
            </div>

            <!-- Search -->
            <div class="flex-1 max-w-md mx-8">
                <div class="relative">
                    <svg class="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                    </svg>
                    <input
                        type="text"
                        placeholder="Поиск по каталогу..."
                        class="w-full pl-10 pr-4 py-2 bg-gray-50 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500"
                    />
                </div>
            </div>

            <!-- User actions -->
            <div class="flex items-center gap-4">
                <a href="{{ path('personal_account') }}" class="flex items-center gap-2 px-3 py-2 border border-gray-300 rounded-lg text-sm hover:bg-gray-50 transition-colors">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                    </svg>
                    Кабинет
                </a>
                <a href="{{ path('personal_cart') }}" class="flex items-center gap-2 px-3 py-2 border border-gray-300 rounded-lg text-sm hover:bg-gray-50 transition-colors">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4m0 0L7 13m0 0l-2.5 5M7 13l2.5 5m0 0L14 13"/>
                    </svg>
                    Корзина
                </a>
            </div>
        </div>

        <!-- Navigation -->
        <nav class="mt-4 pt-4 border-t border-gray-100">
            <div class="flex items-center gap-8">
                <a href="{{ path('index') }}" class="transition-colors {% if app.request.get('_route') == 'index' %}text-red-600{% else %}text-gray-600 hover:text-gray-900{% endif %}">
                    Главная
                </a>
                <a href="{{ path('catalog') }}" class="transition-colors {% if app.request.get('_route') == 'catalog' %}text-red-600{% else %}text-gray-600 hover:text-gray-900{% endif %}">
                    Каталог
                </a>
                <a href="/about" class="transition-colors {% if app.request.get('_route') == 'text' %}text-red-600{% else %}text-gray-600 hover:text-gray-900{% endif %}">
                    О нас
                </a>
                <a href="{{ path('news_list') }}" class="transition-colors {% if app.request.get('_route') == 'news_list' %}text-red-600{% else %}text-gray-600 hover:text-gray-900{% endif %}">
                    Новости
                </a>
                <a href="{{ path('contacts') }}" class="transition-colors {% if app.request.get('_route') == 'contacts' %}text-red-600{% else %}text-gray-600 hover:text-gray-900{% endif %}">
                    Контакты
                </a>
            </div>
        </nav>
    </div>
</header>

<!-- Main content -->
<div class="flex flex-1">
    {% if show_sidebar|default(false) %}
        <!-- Product Sidebar -->
        <aside class="w-64 bg-white border-r border-gray-200">
            <div class="p-4">
                <div class="bg-red-600 text-white p-3 rounded-lg mb-6">
                    <h2 class="font-semibold">Каталог продукции</h2>
                </div>

                <nav class="space-y-1">
                    {% for category in catalogCategories|default([]) %}
                        <div>
                            <button class="w-full flex items-center justify-between p-2 text-left hover:bg-gray-50 rounded-lg transition-colors category-toggle"
                                    data-category="{{ loop.index0 }}">
                                <div class="flex items-center gap-2">
                                    <span class="text-lg">{{ category.icon }}</span>
                                    <span class="text-sm">{{ category.name }}</span>
                                </div>
                                <svg class="w-4 h-4 transform transition-transform category-chevron" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                                </svg>
                            </button>

                            <div class="ml-6 mt-1 space-y-1 category-subcategories hidden">
                                {% for subcategory in category.subcategories %}
                                    <a href="#" class="block w-full text-left text-xs text-gray-600 py-2 px-2 hover:bg-gray-50 rounded transition-colors">
                                        {{ subcategory }}
                                    </a>
                                {% endfor %}
                            </div>
                        </div>
                    {% endfor %}
                </nav>

                <div class="mt-8 p-4 bg-gray-50 rounded-lg">
                    <h3 class="font-semibold text-sm mb-2">Будьте всегда в курсе!</h3>
                    <p class="text-xs text-gray-600 mb-3">Узнавайте о скидках и акциях первыми</p>
                    <div class="space-y-2">
                        <input type="email" placeholder="Ваш e-mail" class="w-full px-3 py-2 text-xs border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500">
                        <button class="w-full px-3 py-2 bg-red-600 hover:bg-red-700 text-white text-xs rounded-lg transition-colors">
                            Подписаться
                        </button>
                    </div>
                </div>
            </div>
        </aside>
    {% endif %}

    <main class="flex-1">
        {% block body %}{% endblock %}
    </main>
</div>

<!-- Footer -->
<footer class="bg-gray-900 text-white mt-20">
    <div class="container mx-auto px-4 py-12">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
            <div>
                <div class="flex items-center gap-3 mb-4">
                    <div class="w-8 h-8 bg-red-600 rounded-lg flex items-center justify-center">
                        <div class="text-white">🏠</div>
                    </div>
                    <h3 class="font-bold">Теплый Дом</h3>
                </div>
                <p class="text-gray-400 text-sm mb-4">
                    Качественные строительные материалы и утеплители с 1999 года
                </p>
            </div>
            <div>
                <h4 class="font-semibold mb-4">Компания</h4>
                <ul class="space-y-2 text-sm text-gray-400">
                    <li><a href="/about" class="hover:text-white transition-colors">О нас</a></li>
                    <li><a href="{{ path('news_list') }}" class="hover:text-white transition-colors">Новости</a></li>
                    <li><a href="{{ path('contacts') }}" class="hover:text-white transition-colors">Контакты</a></li>
                </ul>
            </div>
            <div>
                <h4 class="font-semibold mb-4">Каталог</h4>
                <ul class="space-y-2 text-sm text-gray-400">
                    <li><a href="#" class="hover:text-white transition-colors">Утеплители</a></li>
                    <li><a href="#" class="hover:text-white transition-colors">Гидроизоляция</a></li>
                    <li><a href="#" class="hover:text-white transition-colors">Пароизоляция</a></li>
                </ul>
            </div>
            <div>
                <h4 class="font-semibold mb-4">Контакты</h4>
                <ul class="space-y-2 text-sm text-gray-400">
                    <li>+7 (495) 545-39-00</li>
                    <li>info@teplydom.ru</li>
                    <li>г. Москва, ул. Промышленная, 10</li>
                </ul>
            </div>
        </div>
        <div class="border-t border-gray-800 mt-8 pt-8 text-center text-sm text-gray-400">
            <p>&copy; {{ "now"|date("Y") }} Теплый Дом. Все права защищены.</p>
        </div>
    </div>
</footer>

{% block javascripts %}
    <script>
        // Category toggle functionality
        document.querySelectorAll('.category-toggle').forEach(button => {
            button.addEventListener('click', function() {
                const categoryIndex = this.getAttribute('data-category');
                const subcategories = this.nextElementSibling;
                const chevron = this.querySelector('.category-chevron');

                if (subcategories.classList.contains('hidden')) {
                    subcategories.classList.remove('hidden');
                    chevron.style.transform = 'rotate(90deg)';
                } else {
                    subcategories.classList.add('hidden');
                    chevron.style.transform = 'rotate(0deg)';
                }
            });
        });
    </script>
{% endblock %}
</body>
</html>
